# Supabase local / GitHub integration config.
# https://supabase.com/docs/guides/deployment/branching/github-integration

project_id = "luyeytfhofbmhwnjfeaa"

[api]
enabled = true
port = 54321
schemas = ["public"]
extra_search_path = ["public", "extensions"]
max_rows = 1000

[db]
port = 54322
major_version = 15

# Local `supabase db reset` only. Disabled: content lives in remote Postgres +
# `migrations/` (already applied on production). Restore from a DB backup if
# you need a full snapshot. Do not reintroduce supabase/seed.sql.
[db.seed]
enabled = false
