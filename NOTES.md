# 已知问题与设计备忘

## 挑战者模式（2026-09-06，核心玩法已定稿、已上线 v1，待实测）

用户：现在的举一反三还是太 easy——增加挑战者模式：随机出现某种语法结构的「怪兽」，需至少说出 **3 句符合该语法结构**的句子（AI 判定）才能击败。**定案：无美术期间不画怪兽也不用占位图，以知识点本身为对手卡**（兽名=知识点名、血槽=3 格句泡）。结构只从已过关知识点随机、波次递进、合格句实时走候选资产管道、无失败设计、纪录最高连胜。设计见 `docs/superpowers/specs/2026-09-06-grammar-challenger-design.md`。

## 词汇记忆 · 词语接龙挑战（2026-09-06，核心玩法已定稿、已上线 v1，待实测）

用户：词汇记忆模块增加挑战模式——**词语接龙**（首尾字母接龙，词卡串链）。纯本地无需 AI：每词 10 秒倒计时、超时即结算、纪录最高链/最高分。词库词即稀有（×2）；词库外真英文单词可接（离线词表 36 万词 + 拼写启发式兜底防乱拼，1 分、无图卡）。设计见 `docs/superpowers/specs/2026-09-06-vocab-word-chain-design.md`。

## 学习模式三阶段构想（2026-09-03）

讨论如何设计系统交互，真正做到语法从识别到替换再到重组，最后实现可灵活运用语法大胆表达的英语水平。

- **初阶 = 四选一 + 输入模式**：对应识别。都是「通过例句掌握语法用法」的学习方式。
- **中阶 = 举一反三**：对应替换。已开通 AI 时，关卡练习句清完后追加开放造句门闩（含已过关重打；只显示知识点、不展示例句）；AI 判定合格才进结算/记通关，合格句写入造句候选。未开通 AI 则跳过门闩。设计见 `docs/superpowers/specs/2026-09-06-grammar-produce-gate-design.md`。
- **中高阶 = 问答模式**：简化版的小故事场景，放在一个章节后。针对章节所学语法知识点和例句，生成相应的提问，让学习者用刚掌握的语法作答。相对输入模式，问答模式会让学习者习惯该语法结构在真实对话中的使用方式，而非只会翻译。
- **高阶 = 场景挑战**：作为挑战的一个门，将多种语法知识点的问答串联为一个小故事场景下的连续对话，带倒计时和AI判定。进一步考验对多种语法知识的灵活运用。

## 词汇报错后用 AI 替换图 / 例句 / 音频（2026-08-27）

用户报错只收集问题，**不自动生成**素材。是否生成由管理员决定；管理员决定要生成时，调用 Skill（类似语法课的 `grammar-content-fix`：拿报错 / 词条 → 按反馈文生图或文生文 + 例句 TTS → 资料齐了再替换 `picture_words` 该行，并把 `ai_corrected` 设为 `true`）。字段已加；Skill、生图、TTS、写回尚未做。
审核如何预览AI生成的图片、音频？

## 夸克浏览器适配

1. 输入模式键盘弹出后输入框未上移（2026-08-27）

夸克浏览器在输入模式下（整句输入），点击输入框弹出键盘后，输入框没有被挤到页面上方，导致上方看不到输入框；苹果 Safari 无此问题。疑似夸克对键盘高度 / visualViewport 的交互与 Safari 不同，待查。

2. 暗黑模式适配（2026-08-27）

夸克浏览器自带的暗黑模式（网页强制暗黑）会影响样式渲染，并导致开门/关门图标（DoorIcon）显示异常——问题已定位，根因不是代码。处理方向（用户定调）：**不是对抗暗黑模式强行还原原样式，而是把暗黑模式适配做好**；至少保证门图标能正常展示。门图标已本地化（2026-08-27，imgur → `public/images/doors/*.png`，`import.meta.env.BASE_URL` 引用），剩余工作仅为暗黑模式适配本身。


## 「我的」设置项分组（2026-08-25 用户评审）

现在「我的」里有很多配置项，但部分属于记忆单词模块、部分属于语法学习与挑战模式；应按模块对配置项做章节/分块分组，让页面更优雅清晰（后续语法游戏模式、难度选择也可能放进配置项，数量增多后不分组会很混乱）。备选方案：重新设计「我的」页面，把设置做成独立单页，由「我的」提供一个设置入口点击进入——这也是大多数 APP 的实现方式。
