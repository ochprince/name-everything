# 已知问题与设计备忘

## 新想法（2026-09-03）

- **例句放到全文语境中要求填空**：练习句不孤句出题，先给一段话的语境，要求在其中填空。
- **成功页的颜色用蓝色**：成功（答对）页配色改为蓝色。
- **四选一可作为初学者模式**：现四选一出题形式可定位为初学者模式（难度分层的方向性设想）。
- **引入大模型判定表达是否合理**：学习者产出的句子不唯一/不完全等于标准句时，由大模型判定表达是否合理——用户认可其价值：**能真正考验替换是否足够灵活**（学习者不背唯一答案，产出合法变体也算对）。方向性设想：判定需同时校验「本关替换点/教学目标是否命中」（防绕开目标知识点写出通顺但无关的句子），判对口径与严格比对如何共存待讨论。

## 词汇记忆：内容有序 + 出示随机（2026-08-28，用户定案）

- **批次内容**：按词性优先级 + 词频排序（`picture_words.sort_order` 已重排，与前端 `WORD_PRIORITY` 同源同序）：动词>名词>代词&冠词>介词>形容词>副词，同词性内词频降序。增量批次（按 sort_order 拉 10 词）与全量目录切片顺序一致。
- **批内出示**：`pickNextCard` 随机抽卡（冷词权重 5 / 温词 2，避免同 tag 连续、避免重复当前卡）——**有意设计**，防按位置死记。用户确认：小的乱序达到记忆效果，保持现状。
- 生成脚本：`scripts/generate-word-priority.py`（前端表）、`scripts/generate-sort-order-migration.py`（DB 重排，含触发器自动 bump version）。

## 单词记忆优先级（2026-08-28，用户定案）

词汇记忆按此优先级展开（口语速成，理论：二语习得）：

**动词 ＞ 名词 ＞ 代词&冠词 ＞ 介词 ＞ 形容词 ＞ 副词**

1. **动词**：句子骨架，无动词无法构成完整句子，复用率最高，优先记搭配。
2. **名词**：填充语义内容，表达人、物、地点，建议绑定微型短句记忆。
3. **代词&冠词**（I/you、a/an/the）：高频功能词，名词造句必备，决定语法正确性。
4. **介词**（in/on/at/for/to）：搭建词与词之间逻辑关系，常和动词、名词固定搭配。
5. **形容词**：修饰名词，属于修饰成分，删掉后句子依旧可以成立。
6. **副词**：修饰动词、形容词，锦上添花，优先级最低。

拓展：抽象名词、生僻专有名词放到最后，阅读认识即可，不用早期强行背诵。

## 词汇报错后用 AI 替换图 / 例句 / 音频（2026-08-27）

用户报错只收集问题，**不自动生成**素材。是否生成由管理员决定；管理员决定要生成时，调用 Skill（类似语法课的 `grammar-content-fix`：拿报错 / 词条 → 按反馈文生图或文生文 + 例句 TTS → 资料齐了再替换 `picture_words` 该行，并把 `ai_corrected` 设为 `true`）。字段已加；Skill、生图、TTS、写回尚未做。

## 苹果 Safari 切回前台偶发游戏失败音效（2026-08-27，已修复待实测）

从其他 App 切回 Safari 时偶尔突然发出游戏失败的声音，非必现。已实施两层修复：
① 911cb11 rAF 单帧增量钳制 250ms（iOS 后台 rAF 暂停，恢复首帧 dt=后台全部时长，一次性扣光剩余时间触发落底失败）；
② 45502c8 页面不可见时不调度界面音效（iOS 后台 AudioContext suspend，此时调度的音效排队到切回前台 resume 后补响——声音层面根治）。
待用户设备实测确认后移除本条目。

## 夸克输入模式键盘弹出后输入框未上移（2026-08-27）

夸克浏览器在输入模式下（整句输入），点击输入框弹出键盘后，输入框没有被挤到页面上方，导致上方看不到输入框；苹果 Safari 无此问题。疑似夸克对键盘高度 / visualViewport 的交互与 Safari 不同，待查。

## 夸克浏览器暗黑模式适配（2026-08-27）

夸克浏览器自带的暗黑模式（网页强制暗黑）会影响样式渲染，并导致开门/关门图标（DoorIcon）显示异常——问题已定位，根因不是代码。处理方向（用户定调）：**不是对抗暗黑模式强行还原原样式，而是把暗黑模式适配做好**；至少保证门图标能正常展示。门图标已本地化（2026-08-27，imgur → `public/images/doors/*.png`，`import.meta.env.BASE_URL` 引用），剩余工作仅为暗黑模式适配本身。

## 练习首页模块准入（2026-08-27）

目前「词汇记忆」「语法学习」默认开放（开门图标 = 可用）。未来可考虑给语法学习等后续模块加准入门槛（例如先完成某前置模块 / 过关数），锁定时用关门图标 + 提示；挑战门已按「过关 / 有收藏」做准入，可复用同一套 `available` + `DoorIcon open` 语义。

## 「我的」设置项分组（2026-08-25 用户评审）

现在「我的」里有很多配置项，但部分属于记忆单词模块、部分属于语法学习与挑战模式；应按模块对配置项做章节/分块分组，让页面更优雅清晰（后续语法游戏模式、难度选择也可能放进配置项，数量增多后不分组会很混乱）。备选方案：重新设计「我的」页面，把设置做成独立单页，由「我的」提供一个设置入口点击进入——这也是大多数 APP 的实现方式。

（原「Grammar 限时下落填槽」的刷分漏洞与难度模式想法已随 0f6e731 解决：每格最多允许选错一次，错第二次立即失败——连点试答案最多试 1 次，难度统一为单一规则，不再分档。）
