# 已知问题与设计备忘

## makeup 单词例句显示不完整（2026-08-27）

词汇「makeup」的例句显示不完整，疑似特殊字符导致截断/换行异常，待查。

## 夸克黑屏：词汇记忆倒计时后 / 复习点击单词（2026-08-27 补充）

夸克浏览器黑屏问题，触发点有两个：① 词汇记忆倒计时结束后画面全黑；② **点击复习里的某个单词也会黑屏**。关键信息：黑屏**不是暗黑模式导致**——已切到正常浅色模式仍复现；苹果 Safari 无此问题。两个触发点疑似同一根因（词汇/图片模块渲染相关），待查。

## 夸克浏览器暗黑模式适配（2026-08-27）

夸克浏览器自带的暗黑模式（网页强制暗黑）会影响样式渲染，并导致开门/关门图标（DoorIcon）显示异常——问题已定位，根因不是代码。处理方向（用户定调）：**不是对抗暗黑模式强行还原原样式，而是把暗黑模式适配做好**；至少保证门图标能正常展示。另：门图标当前是 imgur 上的 mask 图（外网依赖），国内/夸克下本就加载不出，适配暗黑模式的同时需把门图标改为本地资源或内联 SVG，彻底摆脱外网（见「图床需翻墙」条目）。

## 图床需翻墙，新用户图片可能加载不出（2026-08-27）

目前选用的图床（imgur）需要翻墙才能访问，国内新用户很可能图片加载不出来。受影响：练习首页 banner（有 `fallback.svg` 兜底）、语法学习列表 banner（无兜底，会破图）、门图标 DoorIcon（无兜底）。备选方向：图片下沉到仓库 `/images/` 或换国内可达图床。

## 练习首页模块准入（2026-08-27）

目前「词汇记忆」「语法学习」默认开放（开门图标 = 可用）。未来可考虑给语法学习等后续模块加准入门槛（例如先完成某前置模块 / 过关数），锁定时用关门图标 + 提示；挑战门已按「过关 / 有收藏」做准入，可复用同一套 `available` + `DoorIcon open` 语义。

## Grammar 限时下落填槽（用户评审，2026-08-25）

1. **刷分漏洞** — 快速连点四个选项即可试出答案：答错不会立即死亡（每句有 3 次机会），第 4 次必过，因此可以用来刷分。
2. **想法：难度模式** —
   - 困难模式：每个选项只有一次错误机会。
   - 地狱模式：每个选项没有错误机会。
3. **「我的」设置项分组** — 现在「我的」里有很多配置项，但部分属于记忆单词模块、部分属于语法学习与挑战模式；应按模块对配置项做章节/分块分组，让页面更优雅清晰（后续语法游戏模式、难度选择也可能放进配置项，数量增多后不分组会很混乱）。备选方案：重新设计「我的」页面，把设置做成独立单页，由「我的」提供一个设置入口点击进入——这也是大多数 APP 的实现方式。
